using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //GameObject handle = GameObject.Find("GameObject");
        //// 入力状態を取得
        //handle.GetComponent<handleclass>().UpdateJoyPad();

        //handle.GetComponent<handleclass>().LimitHandle();
        //Debug.Log(handle.GetComponent<handleclass>().LimitHandle());
        //handle.GetComponent<handleclass>().Pedal(handleclass.Pedals.accelerator);
        //Debug.Log(handle.GetComponent<handleclass>().Pedal(handleclass.Pedals.accelerator));
        //handle.GetComponent<handleclass>().Pedal(handleclass.Pedals.brake);
        //Debug.Log(handle.GetComponent<handleclass>().Pedal(handleclass.Pedals.brake));
        //if (handle.GetComponent<handleclass>().Button(handleclass.Buttons.A)) { Debug.Log("A"); }

        //if (handle.GetComponent<handleclass>().Button(handleclass.Buttons.B)){ Debug.Log("B"); }

        //if (handle.GetComponent<handleclass>().KeepButton(handleclass.Buttons.C)){ Debug.Log("C"); }
            
        //if (handle.GetComponent<handleclass>().KeepButton(handleclass.Buttons.X)) {Debug.Log("X"); }
            
        //if (handle.GetComponent<handleclass>().KeepButton(handleclass.Buttons.Y)) {Debug.Log("Y"); }
            
        //if (handle.GetComponent<handleclass>().KeepButton(handleclass.Buttons.Z)) {Debug.Log("Z"); }
            
        //if (handle.GetComponent<handleclass>().KeepButton(handleclass.Buttons.ShiftDown)) {Debug.Log("ShiftDown"); }
            
        //if (handle.GetComponent<handleclass>().KeepButton(handleclass.Buttons.ShiftUp)) {Debug.Log("ShiftUp"); }
    }
}
